# Lecture 7 validation

Validated September 8, 2026.

- Package validator: 17 slides, 3 problem parts, 3 IDE checkpoints, 11 unique concept IDs, 3 handout pages, 3 exact Java source matches, and 11 refresher concepts. No failures.
- Student prompts: short requirements with exact output, no prescribed solution steps; Problems 2 and 3 explicitly extend the previous wallet program. All problem-copy text matches its displayed statement and output.
- Java: all three programs compiled together and produced the exact documented output. Boundary checks passed for zero balances/additions, repeated additions, repeated accessors, independent objects, and shared-reference behavior. Twelve additional modified-program checks verified documented error causes and extension outputs.
- Handout: complete code is visible and syntax-colored without network dependencies. Three code buttons and three problem buttons copied exact text over HTTP and file URLs. The native clipboard fallback passed, including forced Clipboard API unavailability; failed copies show a usable recovery message.
- Handout print: exactly 3 A4 pages. Complete code, recovery notes, and footers appear on every page. Controls are hidden. Content-to-footer gaps are 46, 113, and 33 CSS pixels, with no code clipping. Pages were rendered and visually inspected.
- Handout reflow: document width equals the viewport at 390px and 720px. Long code lines scroll inside their code panels rather than widening the page.
- Supporting slides: all 17 slides fit at 1440×900 and 1280×720 after all reveals; screenshots and contact sheets visually reviewed. 27 interaction checks and 37 traversal/responsive checks passed, including keyboard navigation, focus protection, URL hashes, progress, final reveal, reduced motion, and mobile widths. Print produces 17 content pages. Browser console: zero errors or warnings. Print transitions are disabled to make every slide immediately visible.
- Refresher: 11 visible abstracts/key rules and 12 native disclosures. Keyboard toggles, open-all/close-all, status announcements, both hash targets, print expansion/state restoration, and all local links pass. Document width matches 390px, 720px, and 1440px with all details open. Print contains all details; its skip link is hidden. No console errors.
- Local links: 95 HTML link/asset targets and fragments checked, with all HTTP requests returning 200. JavaScript syntax and Git whitespace checks passed.
- Source handling: the licensed PDF, extracted text, source renders, test classes, browser screenshots, and print proofs remain in ignored locations. The release contains original teaching material and the portable authoring skill.

The instructor pass confirms one coherent 30-minute progression: a balance, an operation that changes it, and two independent objects. The independent-study pass confirms visible definitions, complete source, exact outputs, and recovery explanations. The source map accounts for all nineteen chapter sections and distinguishes selected concepts from deferred depth.
