# Lecture 8 validation

Validated September 14, 2026.

- Package validator: 16 supporting slides, 3 contiguous problem parts, 3 IDE checkpoints, 10 concept slides and matching Java IDs, 3 handout pages, 3 runnable Java files; no missing links or visual-token failures.
- Java: all three files compile together. Exact normal outputs match the student sheet, handout, and checkpoint slides.
- Handout: copied code text matches each `.java` file, including final newline; Copy problem and Copy complete code controls report success over local HTTP. Chrome printed exactly three A4 pages; visual inspection confirmed all code, instructor notes, and footers remain visible.
- Slides: checked 1280×720; all 16 slides fit. Home, End, ArrowRight, URL hash, counter, and final-answer reveal worked.
- Refresher: all 10 details open from the control; status reports 10 open; at 390px wide, page horizontal overflow is zero.
- The root course homepage and README link to Lecture 8. The licensed source PDF remains outside the new package and is Git-ignored in its prior private location.

The three problems assume nonnegative whole-SAR amounts, enough source balance for the stated transfer, and `int` totals without overflow. Transaction validation is deferred so this lecture stays focused on Java object semantics.
